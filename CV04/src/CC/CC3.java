package CC;

import java.math.BigDecimal;
import java.math.MathContext;
import java.math.RoundingMode;

public class CC3 {
//    public static String bigFib(long n){
//
//
////        BigDecimal sqrt5 = new BigDecimal(5).setScale(470, RoundingMode.HALF_UP).sqrt(new MathContext(470));
////
////        BigDecimal bigDecimalX = new BigDecimal(1).add(sqrt5).divide(new BigDecimal(2)).pow((int) n);
////        BigDecimal bigDecimalY = new BigDecimal(1).subtract(sqrt5).divide(new BigDecimal(2)).pow((int) n);
////
////        BigDecimal divide = bigDecimalX.subtract(bigDecimalY).divide(sqrt5);
////        return divide.toBigInteger().toString();
//    }

}